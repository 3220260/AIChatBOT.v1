{
  "name": "verbot-synetelas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Synetelas FAQ chatbot with Gemini backend and static chat UI.",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "check": "node --check api/chat.js && node --check scripts/dev-server.js"
  },
  "dependencies": {
    "@google/generative-ai": "0.24.1"
  }
}
