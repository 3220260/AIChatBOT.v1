{
  "name": "Sofia",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sofia FAQ chatbot with Gemini backend and static chat UI.",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "test": "node --test",
    "check": "node --check api/chat.js && node --check scripts/dev-server.js && node --check lib/normalize.js && node --check lib/faq-search.js && node --check lib/knowledge.js && node --check lib/memory.js && node --check lib/prompt.js && node --check lib/rate-limit.js && node --check lib/upstash-redis.js && node --check public/chat.js && npm test"
  },
  "dependencies": {
    "@google/generative-ai": "0.24.1"
  }
}
