{
  "name": "Sofia",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sofia FAQ chatbot with Gemini backend and static chat UI.",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "test": "node --test",
    "check": "node --check api/chat.js && node --check scripts/dev-server.js && npm test"
  },
  "dependencies": {
    "@google/generative-ai": "0.24.1"
  }
}
